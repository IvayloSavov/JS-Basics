function projectsCreation(architectName, numberOfProjects) {
    let timeNeedForOneProject = 3;
    console.log(`The architect ${architectName} will need ${numberOfProjects * timeNeedForOneProject} hours to complete ${numberOfProjects} project/s.`);
}
projectsCreation("Ivaylo", 3)
